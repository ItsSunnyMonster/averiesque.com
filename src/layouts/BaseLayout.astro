---
import Cursor from "../components/Cursor.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import { ClientRouter } from "astro:transitions";

import "../styles/global.scss";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<html lang="en" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Jersey+10&family=JetBrains+Mono:ital,wght@0,100..800;1,100..800&family=Lexend:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <script is:inline>
      const key = "theme-preference";

      document.documentElement.dataset.theme =
        localStorage.getItem(key) ??
        (matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light");
    </script>
    <ClientRouter />
  </head>
  <body transition:animate="none">
    <Cursor />
    <Nav />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style lang="scss">
  @use "../styles/themes";
  @use "../styles/mixins/fonts";

  html,
  body {
    margin: 0;
    width: 100vw;
    overflow-x: clip;
    height: 100%;
  }

  body {
    background-color: var(--c-mantle);
    color: var(--c-text);

    position: relative;

    @include fonts.body;

    font-size: clamp(1em, 0.875rem + 0.5vw, 1.25rem);

    display: flex;
    flex-direction: column;
    align-items: center;
    transition:
      background-color 300ms cubic-bezier(0.4, 0, 0.2, 1),
      color 300ms cubic-bezier(0.4, 0, 0.2, 1);
  }

  // @supports (scrollbar-width: auto) {
  //   body {
  //     scrollbar-color: var(--c-overlay0) transparent;
  //     // scrollbar-width: thin;
  //   }
  // }

  // @supports selector(::-webkit-scrollbar) {
  //   ::-webkit-scrollbar {
  //     width: 6px;
  //     marign-right: 10px;
  //   }

  //   ::-webkit-scrollbar-track {
  //     background: transparent;
  //   }

  //   ::-webkit-scrollbar-thumb {
  //     background: var(--c-overlay0);
  //     border-radius: 999px;
  //     transition: background 0.2s;
  //   }

  //   ::-webkit-scrollbar-thumb:hover {
  //     background: var(--unenergized-accent);
  //   }
  // }

  // html {
  //   // scrollbar-gutter: stable;
  // }

  main {
    --padding-x: 2rem;
    max-width: calc(75ch + var(--padding-x) * 2);
    width: 100%;
    padding: 1.5rem var(--padding-x);
    margin-top: 7rem;
  }
</style>

<script src="../scripts/glow_link.ts"></script>
