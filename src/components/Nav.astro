---
import Logo from "./Logo.astro";

interface Props {}
---

<header>
  <div class="content-container">
    <div class="left-container">
      <Logo size={60} />
      <a href="/" class="prose">sunny_monster</a>
    </div>
    <div class="right-container"></div>
  </div>
</header>

<style lang="scss">
  @use "../styles/mixins/fonts";

  a {
    @include fonts.code;
  }

  header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    width: 100svw;

    padding: 1.5rem 2rem;

    display: flex;
    justify-content: center;
    align-items: center;

    // transition: all 200ms ease-in-out;

    border-bottom: 2px solid #ffffff00;
    background-color: var(--c-mantle);

    z-index: 10;
  }

  .content-container {
    max-width: 64rem;
    width: 100%;

    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .left-container {
    display: flex;
    align-items: center;
    gap: 0.75rem;

    > a {
      font-size: 1.5rem;
      font-weight: 480;
    }
  }
</style>
